var chocolateBars = [
  "snickers",
  "hundred grand",
  "kitkat",
  "skittles"
];

function addElementToBeginningOfArray(array, element) {
  var newArray new Array();
  newArray.push(element);
  newArray.push(array);
  array = newArray
}
